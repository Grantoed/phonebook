import styled from '@emotion/styled';
import {
  color,
  space,
  layout,
  typography,
  flexbox,
  border,
} from 'styled-system';

export const Box = styled('div')(
  color,
  space,
  layout,
  typography,
  flexbox,
  border
);
